{
  "name": "historiska-roster-redirect",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "if not exist dist mkdir dist && copy index.html dist\\index.html",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}